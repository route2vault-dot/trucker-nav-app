/* Optional: copy this file to config.js and paste your key to skip
   entering it in the app's settings. config.js is git-ignored so the
   key never ends up in a public repository.
   Get a free key at https://openrouteservice.org (Sign up → Dashboard → Request a token). */

window.STN_CONFIG = {
  orsKey: "PASTE-YOUR-OPENROUTESERVICE-KEY-HERE"
};
